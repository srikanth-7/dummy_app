import 'package:flutter/material.dart';

class PageAnimationView extends StatelessWidget {
  const PageAnimationView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
